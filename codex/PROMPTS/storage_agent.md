
# Prompt: Storage Agent

Implement the immutable chunk store, manifest builder, and integrity verification path. The output must support resumable transfer and later receipt generation. Avoid UI concerns. Add unit and property tests for manifest determinism and chunk ordering.
