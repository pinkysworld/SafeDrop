
# Prompt: Evidence Agent

Implement append-only audit events, signed receipts, and export bundle generation. Design the APIs so that a later zero-knowledge wrapper can consume existing receipts without changing the underlying event semantics.
