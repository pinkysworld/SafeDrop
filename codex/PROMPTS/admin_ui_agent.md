
# Prompt: Admin UI Agent

Build an explanation-first admin console. Every cryptographic object shown in the UI must have a human-readable summary. Include status cards for upload state, reachability path, expiry state, and evidence export readiness.
